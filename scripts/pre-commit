#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
"$ROOT/scripts/update-words-index.sh"

if ! git diff --quiet -- words/index.json; then
  git add words/index.json
  echo "pre-commit: staged words/index.json"
fi
